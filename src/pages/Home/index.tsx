import { View, Text } from 'react-native';
import * as styled from './styles';
import { Modal } from '../../components/Modal';

export const Home = () => {
  return (
    <styled.background>
      <Modal />
    </styled.background>
  );
};
